// import {LocationSearch} from '../features/CreatePage/LocationSearch';

describe('LocationSearch', () => {
  test('displays search results');
  test('displays empty state when no results are returned');
});
